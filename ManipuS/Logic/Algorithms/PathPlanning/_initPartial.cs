using System;

namespace Logic
{
    // class, containing all the algorithms for path planning
    static partial class PathPlanner
    {
        // class-global instance of a random numbers generator (RNG)
        internal static Random Rng = new Random();
    }
}